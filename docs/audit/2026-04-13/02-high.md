## High (fix this sprint)

- [ ] `src/features/auth/components/SignUpFlow.tsx:310` Signup collects `municipality`, but submit only passes name/email/password/phone (`src/features/auth/components/SignUpFlow.tsx:156-161`). `registerCitizen()` stores no municipality (`src/domains/citizen/services/auth.service.ts:25`) even though `UserProfile` supports it (`src/shared/types/auth.types.ts:93`). Either persist it or remove the step.
- [ ] `src/features/profile/components/MyReportsList.tsx:112` Report tracking/linking expects `reporterUserId`/`reporterPhone` fields and composite indexes, but the write paths do not populate them consistently (queue writes `reporterContact.phone` at `src/features/report/hooks/useReportQueue.ts:162-166`), and `firestore.indexes.json` is empty (`firestore.indexes.json:2`). This feature will fail in production.
- [ ] `src/features/report/hooks/useReportQueue.ts:61` Queue state and `isSyncing` are local per hook instance; the hook is used in multiple components (`src/app/navigation.tsx:15`, `src/features/report/components/ReportForm.tsx:133`). Multiple instances can sync in parallel and interleave IndexedDB updates; move sync locking to a singleton/context/store.
- [ ] `src/features/feed/hooks/useFeedReports.ts:44` Infinite-scroll pagination is broken: `pageParam` is accepted but ignored, and `fetchFeedReports()` always returns the same first page. Implement Firestore cursor pagination (`startAfter`) or remove “infinite” behavior.
- [ ] `src/app/App.tsx:65` Alert scoping assumes `useUserContext()` provides municipality/role, but `UserProvider` is never mounted. `AlertList` calls `useUserContext()` at `src/features/alerts/components/AlertList.tsx:19` and passes those to `useAlerts()` (`AlertList.tsx:21`), but the values will be defaults.
- [ ] `src/features/alerts/components/AlertList.tsx:42` Refresh UX is misleading: `useAlerts().refetch` is a no-op (`src/features/alerts/hooks/useAlerts.ts:152-155`). Either remove refresh or make it recreate listeners / re-load cache intentionally.
- [ ] `src/domains/municipal-admin/services/firestore.service.ts:253` Client code writes alerts directly to Firestore, bypassing the callable `createAlert` function that implements stronger server-side checks and audit logging (`functions/src/createAlert.ts:29`). Decide one canonical path and remove the other.
- [ ] `public/firebase-messaging-sw.js:9` Push notifications are not real: SW contains placeholder Firebase config (`public/firebase-messaging-sw.js:10-16`) and the hook stores a `mock-fcm-token` (`src/features/alerts/hooks/usePushNotifications.ts:46`). End-to-end messaging is incomplete.
- [ ] `src/app/routes.tsx:13` Router has no auth guards for screens that rely on authenticated reads, while Firestore rules require auth for reports/alerts (e.g. `firestore.rules:91`, `firestore.rules:249`). Anonymous navigation will hit broken flows; add protected routes or anonymous-safe fallbacks.
- [ ] `package.json:49` Dependency risk: `npm audit` (captured 2026-04-13) reports 21 vulnerabilities including 6 high and 2 moderate. Directly implicated packages include `firebase-tools`, `vite`, `vite-plugin-pwa`. See `08-dependency-audit.md`.

