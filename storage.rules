rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Placeholder rules - Phase 1 will implement proper security
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
