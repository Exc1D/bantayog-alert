// Stubs — replaced by real types in later phases.
// DO NOT import from here in new code — real types land in their
// respective phase files (e.g. user.ts in Phase 1, report.ts in Phase 2).
export type _ReportStub = never // Phase 2
export type _DispatchStub = never // Phase 4
export type _UserStub = never // Phase 1
export type _AlertStub = never // Phase 5
export type _AuditStub = never // Phase 8
export type _CoordinationStub = never // Phase 5/6
export type _SmsStub = never // Phase 6
export type _SystemStub = never // phase-of-first-use
