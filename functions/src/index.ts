// Cloud Functions v2 entry point.
// Phase 0: no triggers exposed. Phase 2+ will export callables, Firestore triggers,
// and HTTP functions.
//
// The empty export keeps `firebase-functions` deploy happy by resolving the module.
export {}
